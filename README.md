# GitHub App - Обработка событий GitHub

GitHub App, который получает информацию о репозитории через GitHub API и обрабатывает события (например, создание issue).

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка GitHub App

### Использование .env файла (рекомендуется)

1. Скопируйте шаблон конфигурации:
```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

2. Откройте файл `.env` и заполните своими значениями

### Вариант 1: Использование GitHub App (рекомендуется)

1. Создайте GitHub App на https://github.com/settings/apps/new
2. Заполните в `.env` файле:
   - `GITHUB_APP_ID` - ID вашего GitHub App (можно найти в настройках App)
   - `GITHUB_APP_PRIVATE_KEY` - Приватный ключ GitHub App (скачайте из настроек App)
   - `GITHUB_INSTALLATION_ID` - ID установки (получите после установки App в репозиторий, опционально)
   - `WEBHOOK_SECRET` - Секретный ключ для проверки подписи webhook (установите в настройках GitHub App)

### Вариант 2: Использование Personal Access Token

Заполните в `.env` файле:
- `GITHUB_TOKEN` - ваш Personal Access Token (создайте на https://github.com/settings/tokens)

### Альтернатива: Переменные окружения

Если не хотите использовать `.env` файл, можете установить переменные окружения напрямую:

```bash
# Windows PowerShell
$env:GITHUB_APP_ID="your_app_id"
$env:GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# Linux/Mac
export GITHUB_APP_ID="your_app_id"
export GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
```

## Запуск

### Вариант 1: Запуск в Docker (рекомендуется)

1. Убедитесь, что у вас установлены Docker и Docker Compose

2. Создайте файл `.env` из шаблона:
   ```bash
   # Windows
   copy env.example .env
   
   # Linux/Mac
   cp env.example .env
   ```

3. Заполните `.env` файл своими значениями

4. Запустите приложение:
   ```bash
   docker-compose up -d
   ```

5. Просмотр логов:
   ```bash
   docker-compose logs -f
   ```

6. Остановка:
   ```bash
   docker-compose down
   ```

Приложение будет доступно на `http://localhost:5000`

### Вариант 2: Запуск без Docker

```bash
python main.py
```

Приложение запустится на `http://localhost:5000`

## Использование

### Получить информацию о репозитории

```bash
GET /repo/<owner>/<repo>
```

Пример:
```bash
curl http://localhost:5000/repo/octocat/Hello-World
```

### Настройка Webhook

1. В настройках вашего GitHub App добавьте Webhook URL:
   ```
   https://your-domain.com/webhook
   ```
   Или для локальной разработки используйте ngrok:
   ```bash
   ngrok http 5000
   ```

2. Установите **Webhook secret** в настройках GitHub App и добавьте его в `.env` как `WEBHOOK_SECRET`

3. Выберите события, которые хотите получать:
   - `Issues` - для обработки создания issue
   - `Installation` - для получения installation_id

4. При установке App в репозиторий, вы получите `installation_id` через webhook

## Обработка событий

### Создание Issue с автоматическим анализом

При создании новой issue в репозитории:

1. GitHub отправляет POST webhook на `/webhook`
2. Приложение проверяет подпись webhook (HMAC SHA256)
3. Если событие - создание issue (`issues` с `action: opened`):
   - Выводит информацию о issue в консоль
   - **Автоматически анализирует issue с помощью LangGraph агента**
   - Создает техническое задание для программиста
   - Выводит ТЗ в консоль

**Пример вывода в консоль:**
```
============================================================
📝 СОЗДАНА НОВАЯ ISSUE
📦 Репозиторий: my-repo
🔗 Полное имя: username/my-repo
#️⃣  Номер issue: #42
📌 Название issue: Исправить баг в функции X
============================================================

🤖 Анализирую issue и создаю техническое задание...

================================================================================
📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
================================================================================
[Здесь будет структурированное ТЗ, созданное AI агентом]
================================================================================
```

### Агент на LangGraph

Приложение использует **LangGraph** для создания AI-агента, который:
- Анализирует описание issue
- Выделяет ключевые требования
- Определяет технические детали
- Создает структурированное техническое задание

ТЗ включает:
- Цель задачи
- Описание проблемы/требования
- Технические требования
- Ожидаемый результат
- Критерии приемки

**Настройка OpenAI API:**
1. Получите API ключ на https://platform.openai.com/api-keys
2. Добавьте в `.env` файл:
   ```
   OPENAI_API_KEY=your_api_key_here
   OPENAI_MODEL=gpt-4o-mini  # или другая модель
   ```

## API Endpoints

- `GET /` - Информация о приложении
- `GET /repo/<owner>/<repo>` - Получить информацию о репозитории
- `POST /webhook` - Webhook для GitHub событий
- `GET /health` - Проверка работоспособности

## Docker

### Сборка образа

```bash
docker build -t github-app .
```

### Запуск контейнера

```bash
docker run -d \
  --name github-app \
  -p 5000:5000 \
  --env-file .env \
  github-app
```

### Просмотр логов

```bash
docker logs -f github-app
```

### Остановка и удаление

```bash
docker stop github-app
docker rm github-app
```

## Пример ответа

```json
{
  "success": true,
  "repository": {
    "name": "Hello-World",
    "full_name": "octocat/Hello-World",
    "description": "Описание репозитория",
    "url": "https://github.com/octocat/Hello-World",
    "language": "Python",
    "stars": 1234,
    "forks": 567
  }
}
```
