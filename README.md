# GitHub Coding Agent - Автоматизированная система разработки

Автоматизированная агентная система для полного цикла разработки программного обеспечения (SDLC) внутри GitHub. Система анализирует задачи, вносит изменения в код, создает Pull Request, запускает CI/CD, анализирует результаты проверок и принимает решение о завершении работы либо повторе цикла разработки.

## 🚀 Как это работает

### Способ 1: Отправка запроса через API

Отправьте POST запрос на `/fix-issue` с ссылкой на issue:

```bash
curl -X POST http://your-server:5000/fix-issue \
  -H "Content-Type: application/json" \
  -d '{
    "issue_url": "https://github.com/owner/repo/issues/1"
  }'
```

Или с указанием owner и repo:

```bash
curl -X POST http://your-server:5000/fix-issue \
  -H "Content-Type: application/json" \
  -d '{
    "owner": "owner",
    "repo": "repo",
    "issue_number": 1
  }'
```

### Способ 2: Автоматическая обработка через webhook

1. Установите GitHub App в ваш репозиторий
2. Настройте webhook URL в настройках GitHub App: `https://your-server/webhook`
3. Создайте issue в репозитории - система автоматически обработает его через webhook

### Что происходит при обработке

1. Система получает данные issue из GitHub
2. AI-агент анализирует issue и создает техническое задание
3. Система определяет файлы для изменения
4. AI-агент исправляет код
5. Создается ветка и Pull Request
6. Запускается CI (проверка синтаксиса, тесты)
7. Reviewer Agent проверяет изменения
8. Если есть проблемы - цикл повторяется (до 10 итераций)

## 🛠️ Как запустить у себя

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd codeagent
```

### 2. Настройка .env

Скопируйте `env.example` в `.env` и заполните необходимые переменные:

```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

Заполните в `.env`:
- `GITHUB_APP_ID` - ID вашего GitHub App
- `GITHUB_APP_PRIVATE_KEY` - Приватный ключ GitHub App
- `WEBHOOK_SECRET` - Секретный ключ webhook
- `OPENAI_API_KEY` - API ключ OpenAI/DeepSeek/OpenRouter
- `OPENAI_MODEL` - Модель для использования

### 3. Запуск

```bash
docker-compose up -d
```

Проверьте логи:
```bash
docker-compose logs -f
```

Приложение будет доступно на `http://localhost:5000`

### Остановка

```bash
docker-compose down
```
